# B1 · บันทึกการแก้บั๊ก 6 จุด — สอบกลางภาค ENGSE203

**ชื่อ–นามสกุล:** นายจิราพัชร อินจันทร์ **รหัส:** 68543210004-6 **Sec:** 2 **ชุด:** B

---

## วิธีกรอก (อ่านก่อนเริ่ม)

กรอกให้ครบ **ทั้ง 6 บั๊ก** · แต่ละบั๊กเขียน 3 อย่างด้วย **คำของตัวเอง** (จะถูกสุ่มถามใน oral)

| คอลัมน์ | ใส่อะไร | ตัวอย่างสิ่งที่เขียน *(บั๊กสมมติ)* |
|---|---|---|
| **ไฟล์ : บรรทัด** | ไฟล์ที่แก้ + เลขบรรทัด | `Foo.jsx : 12` |
| **สาเหตุ** | **ทำไม** มันทำงานผิด (ไม่ใช่แค่ "พิมพ์ผิด") | "ใส่วงเล็บท้ายชื่อฟังก์ชัน เลยถูกเรียกทันทีตอน render" |
| **แก้อย่างไร** | เปลี่ยน**จากอะไร**เป็น**อะไร** | "เอาวงเล็บออก จาก `onClick={fn()}` เป็น `onClick={fn}`" |

> เคล็ด: เลขบรรทัดดูได้จากแถบซ้ายใน VS Code หรือจากข้อความ error ใน Console (เช่น `RequestList.jsx:9`)

---

## ตัวอย่างรูปแบบการเขียน

> ⚠️ นี่คือ **บั๊กสมมติเพื่อสอนวิธีเขียน** — **ไม่ใช่** บั๊กจริงในข้อสอบ

| อาการ | ไฟล์ : บรรทัด | สาเหตุ | แก้อย่างไร |
|---|---|---|---|
| กดปุ่มไม่ได้ แต่พอโหลดหน้าเสร็จฟังก์ชันทำงานเองทันที | `Foo.jsx : 12` | เขียน `onClick={handleClick()}` มีวงเล็บ → JavaScript **เรียกฟังก์ชันทันที**ตอน render แล้วเอาค่าที่ return มาเป็น handler | เอาวงเล็บออก เป็น `onClick={handleClick}` (ส่งตัวฟังก์ชัน ไม่ใช่ผลลัพธ์) |

---

## บันทึกของฉัน — กรอกทั้ง 6 บั๊ก

| # | อาการ (ให้มาแล้ว) | ไฟล์ : บรรทัด | สาเหตุ (ทำไมผิด) | แก้อย่างไร |
|---|---|---|---|---|
| 1 | Console เตือนสีเหลืองเรื่องรายการ (list) ที่แผงสรุป | SummaryPanel.jsx:13  | map() วนลูปสร้าง Element ใน React จำเป็นต้องกำหนด attribute key ที่มีค่า unique ให้กับ Element ตัวนอกสุด  | เพิ่ม key={key} ที่ <article> |
| 2 | ตัวเลข "เสร็จสิ้น" ในแผงสรุปไม่ตรงกับที่เห็นจริง | SummaryPanel.jsx:6 | การสะกด Key หรือ Map ค่าสถานะไม่ตรงกันระหว่างไฟล์ | วิธีแก้: ตรวจสอบ Key ใน object items ของ SummaryPanel.jsx ให้ตรงกับ Key ที่ส่งมาจาก State/Service |
| 3 | กดตัวกรองสถานะใด ๆ แล้วผลไม่เปลี่ยน (เหมือนกรองไม่ทำงาน) | DashboardPage.jsx:55 | RequestDetailPage.jsx ไม่ได้ใช้ useEffect ที่คอยดักจับการเปลี่ยนแปลงของ id จาก useParams() ทำให้เมื่อ URL เปลี่ยน Component ไม่ยอม re-fetch/re-render | เพิ่ม id เข้าไปใน Dependency Array ของ useEffect |
| 4 | เปลี่ยน URL `REQ-101` → `REQ-102` แล้วข้อมูลไม่เปลี่ยน | RequestDetailPage.jsx:24 | สาเหตุ: RequestDetailPage.jsx ไม่ได้ใช้ useEffect ที่คอยดักจับการเปลี่ยนแปลงของ id จาก useParams() ทำให้เมื่อ URL เปลี่ยน Component ไม่ยอม re-fetch/re-rende | เพิ่ม id เข้าไปใน Dependency Array ของ useEffect |
| 5 | กด "ลบ" แล้วการ์ดหาย แต่ตัวเลขในแผงสรุปไม่ลด | DashboardPage.jsx:51 | ฟังก์ชันลบ (onDelete) ไปอัปเดตเฉพาะ State ของรายการคำร้อง (requests) แต่ไม่ได้คำนวณหรืออัปเดต State ของ summary ใหม่ | .ให้คำนวณค่า summary แบบ dynamic จาก State requests โดยตรง (derived state) แทนการแยก State ไว้ต่างหาก |
| 6 | กด "ลบ" แล้วหน้าพัง/ว่างเปล่า | DashboardPage.jsx:64 | ลืมใส่ await ตอนเรียก deleteRequest() ใน DashboardPage.jsx | ใส่ await ที่หน้า deleteRequest(requestId) |

---

## เช็คก่อนส่ง

- [ ] กรอกครบทั้ง 6 บั๊ก (ไฟล์:บรรทัด · สาเหตุ · แก้อย่างไร)
- [ ] เขียนด้วยคำของตัวเอง ไม่ใช่ก๊อปคำอธิบายจาก AI มาตรง ๆ
- [ ] ยืนยันแล้วว่าอาการทั้ง 6 หายจริงหลังแก้ (`npm run dev` แล้วลองทีละอาการ)
